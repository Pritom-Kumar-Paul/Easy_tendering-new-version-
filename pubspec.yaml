name: flutter_application_7
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0' # Eita terminal-er chahida onujayi set kora holo

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase Services
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.3
  firebase_messaging: ^16.0.3
  
  # Image & Media Services
  cloudinary_public: ^0.23.1 # Sudhu ekbar-i thakbe ebong null-safe version
  file_picker: ^10.3.3
  url_launcher: ^6.3.2
  signature: ^6.3.0

  # Notifications
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true